# Locate the .Xauthority file on your Ubuntu system.
root@kali:/home# find / -type f -name .Xauthority 2>/dev/null

# Now obtain the sha-256 digest of the .Xauthority file using openssl.
root@kali:/home# openssl dgst -sha256 /../../.Xauthority -out output.txt
